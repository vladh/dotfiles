#!/bin/sh -eu

fd -e jpg | while read fname; do
    dir=$(dirname "$fname")
    base=$(basename "$fname")
    outdir="../600x/$dir"
    mkdir -p "$outdir"
    echo "$fname"
    convert "$fname" -resize 600x\> "$outdir/$base"
done
